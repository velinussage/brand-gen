# Add a material type

1. Add the material key mapping in `mcp/brand_iterate.py`.
2. Add or extend its material policy.
3. Update reference-role-pack coverage in `data/reference_role_packs.json`.
4. Add any prompt-review rules if the material needs new constraints.
5. Validate with `pipeline` and `show-session-summary`.
